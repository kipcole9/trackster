Building a new server.

1.  Update chef gem
2.  Building the hosts file fails the first time.  Fix that.
3.  Building nginx from source starts the nginx service.  We don't always want that - fix it.

Table formatter

*	Add no_set_on_blank and unknown_on_blank as arguments to numeric formatters; :options => ....

*	Add linkability to formatters.  Need to work out how to specify the url such that can plug in 
relevant parameters?

Missing Reports

*	Content Overview

*	Traffic Source Overview

*	Loyalty Overview

*	Internal Search Overview

Charts

*	Multiseries for charts as standard

* 	Spline, regression, average lines

*	Plotband for campaign start and finish

Navigation

*	overflow: none on navigation divs to prevent scroll bars ever

*	<li> items overflow: elipsis

Reports

*	Default translation for period and time group should be blank (avoid translation error)

Campaign Reports

*	Add additional dimensions like device in campaign reporting. 

Templates

*	Collapse into one template for the site

*	Ajax loading of page content (hijack links and ajax load) so works as "Web App" on iPad

Navigation

*	Navigation should be a popover on touch devices and content should fill the whole screen

Issues

*	Print button in IE7 (looks like javascript not working)

*	Click map - better way to highlight no links?

*	Graphs not appears in IE 7 (javascript)

*	Documentation
