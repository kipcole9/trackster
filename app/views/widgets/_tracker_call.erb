<script>
  tracker = new _tks('<%= Trackster::Config.tracker -%>');
  <%= @set_user -%>tracker.trackPageview();
  if (startLoad) {
    loadTime = (new Date).getTime() - startLoad;
    tracker.trackEvent('page','load',null,loadTime);
  }
</script>

